# 司机智能召回系统 PRD

## 一、文档信息

| 项目 | 内容 |
|------|------|
| 产品名称 | 司机智能召回系统 |
| 版本 | V1.0 |
| 状态 | MVP 开发完成 |
| 日期 | 2026-07-31 |

---

## 二、背景与目标

### 2.1 业务背景

网约车平台在日常运营中面临司机流失问题：部分司机由于个人原因、季节性波动或竞争对手吸引，暂时停止出车。平台需要一个智能化的召回机制，自动识别**高召回潜力**的离线司机，并生成个性化触达策略，帮助运营人员高效执行召回。

### 2.2 核心目标

1. **自动化**：每天自动分析前一天数据，无需人工筛选
2. **精准化**：用神经网络评估每个司机的召回潜力，替代人工经验判断
3. **个性化**：用大模型为高潜力司机生成定制化触达话术
4. **可操作**：运营人员在一个页面完成"查看 → 筛选 → 触达"的全流程

---

## 三、系统流程

```
数据仓库(T-1) → 前置过滤 → 特征工程 → 神经网络打分 → 筛选高潜力 → LLM生成策略 → 运营后台
```

### 3.1 管道六步

| 步骤 | 名称 | 输入 | 输出 | 说明 |
|------|------|------|------|------|
| Step1 | 数据拉取 | 数仓多表 JOIN | `t_driver_daily_snapshot` | 拉取 T-1 日司机状态、订单、收入数据 |
| Step2 | 前置过滤 | 快照表 | 过滤后的司机子集 | 近7天在线≥1次 + 最后完单≤7天 + 区域供需比≥1.5 |
| Step3 | NN推理 | 过滤后司机的特征向量 | 召回潜力分(0-100) | 特征工程 → MLP推理 → 写入`t_recall_list` |
| Step4 | 高潜筛选 | 召回分 | 高潜司机名单 | 支持绝对阈值(>60分)或Top-N两种模式 |
| Step5 | 策略生成 | 司机行为数据 | 个性化话术+渠道 | 有API Key用大模型，无Key用本地规则引擎 |
| Step6 | 最终产出 | 完整名单 | KPI+趋势 | 计算汇总指标，更新`t_recall_kpi`和`t_daily_trend` |

### 3.2 过滤条件

| 条件 | 参数 | 默认值 |
|------|------|--------|
| 近7天在线次数 ≥ N | `recent-active-days` | 1 |
| 最后完单距今天数 ≤ N | `max-last-order-days` | 7 |
| 所在区域供需比 ≥ N | `supply-demand-ratio-threshold` | 1.5 |

### 3.3 两种运行模式

| 模式 | 触发方式 | 数据来源 | 场景 |
|------|----------|----------|------|
| 日常定时 | Cron `0 0 7 * * ?` | 数仓 T-1 离线 | 每天早上自动执行 |
| 应急实时 | API: `/emergency/recall` | MCP实时查询 | 突发缺车 |

---

## 四、系统架构

### 4.1 技术栈

**后端**：Java 17 / Spring Boot 3.3 / MyBatis-Plus 3.5 / MySQL 8.0

**前端**：Vue 3.4 / Element Plus 2.9 / ECharts 5.6 / Pinia 2.2 / Axios

**外部集成**：数仓(JDBC) / 神经网络(REST) / 大模型 DeepSeek(Anthropic兼容API) / MCP服务(JSON-RPC)

### 4.2 数据库

| 表名 | 用途 | 数据流向 |
|------|------|---------|
| `t_driver_daily_snapshot` | 司机每日快照 | 数仓写入 / 管道读取 |
| `t_recall_pipeline_log` | 管道执行日志 | 管道写入 |
| `t_recall_list` | 召回名单（核心产出） | 管道写入 / 运营读取 |
| `t_recall_kpi` | KPI缓存 | 管道写入 / 仪表盘读取 |
| `t_emergency_recall` | 应急召回会话 | 应急API写入 |
| `t_daily_trend` | 每日趋势 | 管道写入 / 图表读取 |
| `shedlock` | 分布式锁 | 框架自动 |

### 4.3 司机基础字段

| 字段 | 说明 |
|------|------|
| driver_id / driver_name / phone | 基本信息 |
| city | 所在城市 |
| online_count_7d | 近7天上线次数 |
| last_order_time | 最后完单时间 |
| daily_orders / morning_peak_orders / evening_peak_orders | 日完单 / 早晚高峰完单 |
| daily_online_hours / morning_peak_online_hours / evening_peak_online_hours | 在线时长 |
| base_income / bonus_income / commission_free_income | 收入构成 |
| supply_demand_ratio | 所在区域供需比 |
| avg_rating / complaint_count / cancel_rate | 服务质量 |
| recall_score | 召回潜力分（NN产出） |
| strategy_script / recommended_channel | 召回话术+渠道（LLM产出） |
| outreach_status | 触达状态（运营操作） |

---

## 五、神经网络设计

### 5.1 特征工程

从原始业务字段计算 10 维特征向量，每维归一化到 [0,1]：

| 维度 | 含义 | 计算方式 |
|------|------|---------|
| 0 | 活跃度 | 在线次数 / 21 |
| 1 | 轨迹得分 | 完单量 / 在线天数 / 8 |
| 2 | 时间偏好 | 高峰订单占比 |
| 3 | 响应率 | 1 - 取消率 |
| 4 | 收入敏感度 | 均单金额 / 50 |
| 5 | 投诉惩罚 | 投诉次数 / 完单量 |
| 6 | 取消率惩罚 | 直接取取消率 |
| 7 | 出勤规律 | 活跃天数 / 7 |
| 8 | 高峰得分 | 高峰订单占比 |
| 9 | 忠诚度 | 评分归一×0.5 + 完单归一×0.3 + 活跃归一×0.2 |

### 5.2 模型架构

三层 MLP：`input(10) → hidden1(16, ReLU) → hidden2(8, ReLU) → output(1, Sigmoid) × 100`

输出：每个司机的召回潜力分（0-100）。

---

## 六、LLM 策略生成

### 6.1 输入

结构化 JSON，包含司机的完整行为画像、活跃度评估、区域数据解读、运营建议标签。

### 6.2 输出

| 字段 | 说明 |
|------|------|
| strategyScript | 30-60字中文个性化话术 |
| recommendedChannel | SMS / PHONE / APP_PUSH |

### 6.3 约束

- 话术**不含任何价格/奖励/金额**相关内容
- 话术基于运力需求侧理由（订单增多、运力紧张）
- 渠道按活跃度推荐：高活跃→外呼/推送，低活跃→短信

### 6.4 兜底策略

未配置大模型 API Key 时，自动切换本地规则引擎，基于司机行为数据生成通用话术。

---

## 七、运营后台

### 7.1 页面布局

```
┌──────────────────────────────────────────────────────┐
│  ● 司机智能召回    [日期选择] [运行管道] [刷新]          │
├──────────────────────────────────────────────────────┤
│  [拉取司机总数]  [过滤后候选]  [预期召回成功率]           │
├──────────────────────────────────┬────────────────────┤
│  筛选栏                          │  城市排名           │
│  ┌────────────────────────────┐ │  上海  │
│  │ 司机列表（勾选/排序/触达）   │ │  北京   │
│  │                            │ │  深圳             │
│  └────────────────────────────┘ │  ...              │
├─────────────────────────────────┴─────────────────────┤
│  近7天趋势              │  触达状态分布                │
└──────────────────────────────────────────────────────┘
```

### 7.2 KPI 卡片

| 指标 | 含义 |
|------|------|
| 拉取司机总数 | T-1日离线司机快照数据总量 |
| 过滤后候选司机 | 满足三个过滤条件的司机数 |
| 预期召回成功率 | 高潜力司机中已生成策略的占比 |

### 7.3 司机列表

| 列 | 说明 | 脱敏规则 |
|----|------|---------|
| 司机 | 姓名 | — |
| 日完单 | 完单量等级 | 少/中/多/很多 + 进度条 |
| 高峰占比 | 早晚峰偏好 | 早>晚/晚>早/均衡 |
| 在线 | 在线时长等级 | 低/中/高 + 进度条 |
| 收入 | 收入水平 | 高/中/低 标签 |
| 召回分 | 0-100 分数 | 真实展示 |
| 召回话术 | LLM生成文案 | 超长省略 |
| 渠道 | 推荐触达方式 | 短信/外呼/推送/微信 |
| 状态 | 触达进度 | 待触达/已触达/已同意/已拒绝 |

### 7.4 操作功能

| 功能 | 说明 |
|------|------|
| 筛选 | 按触达状态、姓名/手机号搜索 |
| 排序 | 按召回分/日完单/在线时长排序 |
| 批量触达 | 勾选多条 → 选择渠道 → 一键发送 |
| 单个触达 | 查看话术 → 选择渠道 → 确认发送 |
| 详情查看 | 侧边抽屉展示司机完整数据+LLM原始输出 |

---

## 八、API 接口

所有接口统一返回 `{"code": 200, "message": "success", "data": ...}`

### 管道控制

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/pipeline/trigger` | 手动触发管道 |
| GET | `/api/v1/pipeline/status/{runId}` | 查询管道状态 |
| GET | `/api/v1/pipeline/last-run` | 最近一次运行结果 |

### 召回名单

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/recall/list` | 分页查询（支持筛选/排序/关键字） |
| GET | `/api/v1/recall/{id}` | 司机详情（含LLM原始响应） |
| PUT | `/api/v1/recall/{id}/outreach` | 单个触达 |
| POST | `/api/v1/recall/batch-outreach` | 批量触达 |

### 仪表盘

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/kpi/summary` | KPI 卡片数据 |
| GET | `/api/v1/chart/trend` | 7日趋势 |
| GET | `/api/v1/chart/distribution` | 触达状态分布 |
| GET | `/api/v1/chart/city-heatmap` | 城市分布数据 |

### 应急召回

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/emergency/recall` | 触发应急召回（多边形坐标） |
| GET | `/api/v1/emergency/status/{sessionId}` | 轮询状态 |
| GET | `/api/v1/emergency/result/{sessionId}` | 获取结果 |

---

## 九、配置项

| 配置 | 默认值 | 说明 |
|------|--------|------|
| `recall.high-potential-threshold` | 60.0 | 高潜力分数阈值 |
| `recall.supply-demand-ratio-threshold` | 1.5 | 供需比最低门槛 |
| `recall.recent-active-days` | 7 | 近N天活跃天数 |
| `recall.max-last-order-days` | 7 | 最后完单最大间隔 |
| `recall.top-n-count` | 0 | Top-N模式：0=不限制 |
| `external.llm.api-key` | — | 大模型API Key |
| `scheduling.daily-recall.cron` | 0 0 7 * * ? | 定时任务cron |

---

## 十、验收标准

- [ ] 管道六步能完整运行，每步结果可追溯
- [ ] NN 特征工程 + MLP 推理产出合理的 0-100 分数
- [ ] 高潜司机能收到个性化召回话术
- [ ] 前端仪表盘 KPI / 表格 / 图表 / 城市排行全部展示
- [ ] 支持筛选、排序、单个触达、批量触达
- [ ] 定时任务每天早上 7 点自动执行
- [ ] 所有外部服务不可用时自动降级（跳过/本地兜底）
- [ ] 数据脱敏：收入/完单/在线时长以等级展示
